# PyBodian 本次更新说明

这次主要围绕桌面歌词、播放退出、自动续播和播放稳定性做修复，目标是让 GUI 播放更接近正常桌面音乐播放器的体验。

## 功能更新

- 新增独立桌面歌词浮窗进程，避免歌词窗口和主 GUI 互相拖累。
- 桌面歌词改为纯透明背景，只展示当前单行歌词，不再铺开整段歌词。
- 歌词浮窗默认放在屏幕偏底部，支持拖动、置顶、锁定和解锁。
- 锁定状态改为极简显示，只保留 `已锁` 与 `解锁`，隐藏关闭、换色等其他按钮。
- 歌词字体和窗口高度调小，并限制历史窗口尺寸，避免旧配置把歌词撑得过大。
- 支持歌曲播放结束后自动续播下一首，并同步切换歌词。

## 问题修复

- 修复按 `q` 退出后 `ffplay.exe` 仍在后台播放的问题。
- 增加 Windows 控制台关闭处理，尽量覆盖直接关终端窗口时的清理。
- 为 `ffplay` 增加 Windows Job Object 绑定，主进程退出时一并回收播放器子进程。
- 播放、暂停、停止、切歌和退出时统一清理残留 `ffplay.exe`，减少后台多首歌同时播放。
- 修复拖动进度条到末尾时，旧歌曲和下一首歌曲同时播放的问题。
- 增加播放请求编号，避免较慢的音频地址加载回来后启动过期歌曲。
- 修复歌词第一行可能不显示的问题。
- 优化歌词浮窗置顶刷新逻辑，降低被桌面或其他窗口压住的概率。

## 性能与体验

- 默认播放音质调整为更轻的 `2` 档，减少高音质地址加载和解码带来的等待。
- 避免重复启动过期播放请求，降低加载慢时出现卡顿和串音的概率。
- 清理调试阶段生成的桌面歌词截图，仓库不再保留临时验证图片。

## Git 忽略更新

- `.gitignore` 新增 `overlay_*.png`，用于忽略桌面歌词调试截图。
- `.bodian/`、`downloads/`、Python 缓存、虚拟环境和日志文件已经在 `.gitignore` 中，无需额外提交本地账号配置或下载内容。
